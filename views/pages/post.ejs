<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title><%= data.title %></title>
    <%- include('../partials/head'); %>
    
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <style>
        .blank{
            width: 12px;
            display: inline-block;
        }

        .board_btn{
            color: #fff !important;
            background-color: #333;
            padding: 2px 8px;
        }
    </style>
</head>
<body>
    <div class="content">
        <%- include('../partials/nav'); %>

        <div class="card border-light mb-3">
            <div class="card-header bg-transparent">
                <h1 class="text-info"><%= data.title %></h1>

                <div class="d-flex justify-content-between">
                    <span class="text-secondary">&nbsp;&nbsp;<%= data.name %></span>

                    
                    <form action="/board/delete/<%= data.num %>" method="GET">
                        <% if(isAuthenticated && currentUser.numid == data.author){ %>
                            <div class="btn-group btn-group-sm" role="group">
                                <a href="/board/edit/<%= data.num %><%= getQueryString() %>" class="btn btn-secondary" role="button">
                                    <i class="fa fa-pencil-square-o"></i>
                                    수정
                                </a>
                                <a href="#" onclick="confirm('삭제하시겠습니까?')?this.parentElement.parentElement.submit():null;" class="btn btn-secondary" role="button">삭제</a>
                            </div>
                        <% }; %>
                    </form>
                    </div>
                </div>
                
            </div>

            <div class="card-body">
              <%= data.content %>
            </div>

            <div class="card-footer bg-transparent d-flex justify-content-end">
                <a href="/board<%= getQueryString() %>" class="board_btn ">
                    <i class="fa fa-bars"></i>
                    <span>목록</span>
                </a>
            </div>
        </div>
    </div>
</body>
</html>